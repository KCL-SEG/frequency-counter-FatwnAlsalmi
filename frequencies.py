"""Frequencies function."""
"""ENTER YOUR SOLUTION HERE!"""

def frequencies(items):
    frequencies = {}
    # Your code goes here
    for value in items:
        count = 0
        for val in items:
            if (value == val)
                count+=1
        frequencies[value] = count
        
    return frequencies
